<Window x:Class="DesktopTracking.SignInWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopTracking"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Title="Авторизация" Height="450" Width="500">
    <Grid>
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Width="300">
            
            <Label Content="Имя пользователя:"/>
            <TextBox x:Name="txbUsername"/>
            <Label Content="Пароль:"/>
            <PasswordBox x:Name="psbPassword"/>
            <Button Content="Войти" Margin="0,5" Click="btnSignIn_Click" x:Name="btnSignIn"/>
            <Button Content="Отмена" Click="btnCancel_Click" x:Name="btnCancel"/>
        </StackPanel>
    </Grid>
</Window>
